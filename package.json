{
  "name": "namore-landing-astro",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "description": "Programmatic SEO build for namore.app \u2014 15K+ per-name landing pages generated from the Supabase names table.",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "node scripts/generate-top-names.mjs && astro build && npm run sitemap",
    "build:test": "BUILD_LIMIT=100 astro build && npm run sitemap",
    "build:mock": "USE_MOCK_DATA=1 astro build && npm run sitemap",
    "sitemap": "node scripts/generate-sitemap.mjs",
    "preview": "astro preview",
    "check": "astro check",
    "astro": "astro"
  },
  "dependencies": {
    "astro": "^4.16.18",
    "@supabase/supabase-js": "^2.45.4",
    "@fontsource-variable/fraunces": "^5.2.5",
    "@fontsource-variable/inter": "^5.2.5",
    "@fontsource-variable/jetbrains-mono": "^5.2.5"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.4",
    "typescript": "^5.6.3"
  }
}
